server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/Isaac192063/pract-mic.git
          search-paths: config-data
  application:
    name: config-service
